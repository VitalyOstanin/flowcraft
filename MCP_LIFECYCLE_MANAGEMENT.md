# Управление жизненным циклом MCP серверов

## Проблема
MCP серверы запускались при старте CLI и работали постоянно, потребляя ресурсы даже когда не используются.

## Решение
Реализовано **ленивое управление MCP серверами** - они запускаются только при выборе конкретного workflow.

## Изменения

### 1. Остановка MCP серверов при смене workflow
```python
# В select_workflow()
if self.current_workflow and self.mcp_manager:
    await self._stop_current_workflow_mcp_servers()
```

### 2. Остановка всех MCP серверов при выходе из CLI
```python
# В start()
if self.mcp_manager:
    await self._stop_all_mcp_servers()
    console.print("MCP серверы остановлены", style="dim yellow")
```

### 3. Новые методы управления
```python
async def _stop_current_workflow_mcp_servers(self):
    """Остановить MCP серверы текущего workflow."""
    
async def _stop_all_mcp_servers(self):
    """Остановить все активные MCP серверы."""
```

## Жизненный цикл MCP серверов

```
CLI Start → No MCP servers running
    ↓
Select Workflow → Start MCP servers for this workflow
    ↓
Change Workflow → Stop previous + Start new MCP servers  
    ↓
Exit CLI → Stop all MCP servers
```

## Преимущества

1. **Экономия ресурсов** - MCP серверы работают только когда нужны
2. **Изоляция** - каждый workflow имеет свои экземпляры MCP серверов
3. **Чистота** - при смене workflow нет "мусора" от предыдущих серверов
4. **Надежность** - гарантированная остановка при выходе из CLI

## Статус

✅ **Реализовано:**
- Ленивый запуск MCP серверов при выборе workflow
- Остановка предыдущих серверов при смене workflow  
- Остановка всех серверов при выходе из CLI
- Асинхронные методы управления жизненным циклом

**Теперь MCP серверы запускаются только когда действительно нужны!** 🎯
